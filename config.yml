title: BewmDewms
description: My course repository
template:
  repo: bewmdewms-template
  name: bewmdewms
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
